function (key, value){
	
	reducedValue = { score: 0, total_votes: 0 };
    
    for (var i = 0; i < value.length; i++){
        reducedValue.score += value[i].score;
    	reducedValue.total_votes += value[i].total_votes;
    }

    return reducedValue;

}
